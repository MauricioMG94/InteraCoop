@page "/interaction/create"

@if (loading)
{
    <Loading />
}
else
{
    <InteractionForm @ref="interactionForm" 
        Interaction="interaction" 
        nonSelectedInteractions="nonSelectedInteractions"
        OnValidSubmit="CreateAsync"
        ReturnAction="Return" 
        FormName="Crear Interacción"
        CardName="Nueva Interacción"/>
        
}