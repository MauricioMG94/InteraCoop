@page "/opportunity/create"

@if (loading)
{
    <Loading />
}
else
{
    <OpportunityForm @ref="opportunityForm" 
        Opportunity="opportunity" 
        nonSelectedOpportunities="nonSelectedOpportunities" 
        OnValidSubmit="CreateAsync" 
        ReturnAction="Return" 
        FormName="Crear oportunidad"/>
}